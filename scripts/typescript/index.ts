const numeros: number[] = [1, 2, 3, 4, 5];
numeros.map((x:number) => x * x).forEach((x: number) => console.log(x));

const persona: {
    nombre: string;
    edad: number;
} = {
    nombre: 'Coderhouse',
    edad: 30
}

console.log(persona);

